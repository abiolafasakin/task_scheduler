The task scheduler allows for scheduling and managing of
tasks to be executed at specified intervals, such as hourly,
daily, or weekly. The script provides options to list scheduled 
tasks, add new tasks, and remove existing tasks.

Requirements:

Install crontab if it is not already installed on your system. To 
install crontab, run the following command in the terminal:

sudo apt-get update
sudo apt-get install cron